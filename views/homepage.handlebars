<div>
    {{#each posts as |post|}}
    <article class="post-preview">
        <div>
            <h3>{{title}}</h3>
            <h5>posted by {{user.username}} on {{format_date created_at}}</h5>
            <a href="/post/{{id}}" class="post btn btn-outlilne-secondary">View post</a>
        </div>
    </article>
    {{/each}}
    </div>